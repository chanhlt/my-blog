---
title: "Hello World — Bài Viết Đầu Tiên"
description: "Bài test ngắn để kiểm tra pipeline MDX hoạt động từ đầu đến cuối."
date: "2026-02-28"
tags: ["intro", "blog"]
---

Chào mừng đến blog của tôi! Bài viết này chứng minh pipeline MDX đã hoạt động.

## Tại Sao Tôi Xây Cái Này

- Nằm trong Git repo
- Dùng Markdown để viết
- Hỗ trợ tô màu code ngay từ đầu

## Ví Dụ Code

Đây là một hàm TypeScript đơn giản:

```typescript
function greet(name: string): string {
  return `Hello, ${name}!`;
}
console.log(greet('World'));
```

## Tiếp Theo

Ở phase tiếp theo mình sẽ thêm database với Drizzle ORM cho view count và subscription.
